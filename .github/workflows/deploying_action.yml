name: 'Deploy'
on: 
    - repository_dispatch
jobs:
    sites-squads:
        name: 'sites-squads'
        runs-on: ubuntu-latest
        steps:
          - name: 'terraform plan'
            run: echo terraform plan
          - name: 'terraform apply'
            run: echo terraform apply
          - name: 'configure'
            run: echo configure
    
